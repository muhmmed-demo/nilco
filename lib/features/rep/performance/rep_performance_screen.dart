import 'package:flutter/material.dart';

class RepPerformanceScreen extends StatelessWidget {
  const RepPerformanceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RepPerformanceScreen')),
      body: const Center(
        child: Text('RepPerformanceScreen - Placeholder', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
