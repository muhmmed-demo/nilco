import 'package:flutter/material.dart';

class RepRouteScreen extends StatelessWidget {
  const RepRouteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RepRouteScreen')),
      body: const Center(
        child: Text('RepRouteScreen - Placeholder', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
